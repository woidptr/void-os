[project]
name = "builder"
version = "0.1.0"
description = "Builder Script"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
]

[tool.setuptools]
packages = ["builder"]
